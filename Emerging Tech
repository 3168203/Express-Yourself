void setup(){
  size(1080,1080);
  background (0);
}

void draw(){
 
pushMatrix();
  fill(random(144), random(233),(455));
  strokeWeight (0);
  translate(width/2, height/2);
  rotate(-frameCount/10);
  scale(random(0.25,5));
  ellipse(100,100,100,100);
popMatrix();

}
